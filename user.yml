---
- name: user
  hosts : all
  tasks:
    - name: Create user
      user:
        name: "{{ user }}" #variables
        state: absent #present-create_user/absent-deleteUser
      become: true

      #ansible-playbook -i hosts.ini user.yml -K  (-K - give opportunities to enter "become_password")

